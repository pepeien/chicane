#include "Controller.hpp"

namespace Chicane
{
    namespace Controller
    {
        Instance::Instance()
        {

        }

        //std::vector<SDL_GameController*> findAvailable()
        //{
        //    std::vector<SDL_GameController*> result;

        //    for (uint32_t i = 0; i < SDL_NumJoysticks(); i++)
        //    {
        //        if (!SDL_IsGameController(i))
        //        {
        //            continue;
        //        }

        //        result.push_back(SDL_GameControllerOpen(i));
        //    }

        //    return result;
        //}
    }
}