#pragma once

#include "Base.hpp"

namespace Chicane
{
    namespace Controller
    {
        class Instance
        {
        public:
            Instance();
        };

        //std::vector<SDL_GameController*> findAvailable();
    }
}