Editor::HeaderMenu {
    position: relative;
    height: 90%;
}

.header-menu {
    height: 100%;
    display: flex;
    overflow: visible;

    &__check {
        display: none;
    }

    &__label {
        margin: auto 0;
        color: $--black;
        font-size: 0.75em;
        transition: color 200ms ease-in-out;
    }

    &__control {
        height: 100%;
        display: flex;
        flex-direction: row;
        gap: 0.45em;
        padding: 0 0.4em;
        overflow: hidden;
        background-color: transparent;
        transition: background-color 200ms ease-in-out;
        border-radius: 5px;

        &:hover {
            cursor: pointer;
            background-color: $--black;

            Text, Icon {
                color: $--white;
            }
        }
    }

    &__shortcut {
        margin: auto 0 auto 1.5em;
        color: $--grey;
    }

    &__chevron {
        width: 0.45em;
        height: 0.45em;
        margin: auto 0 auto auto;
        color: $--grey;
    }

    &__list {
        position: absolute;
        display: flex;
        flex-direction: column;
        gap: 0.25em;
        overflow: visible;
        margin: calc(100% + 0.15em) 0 0 0;
        padding: 0.5em;
        width: 14em;
        border-radius: 5px;
        background-color: $--dark-grey;
        z-index: 99;

        Editor::HeaderMenu {
            width: 100%;
            height: auto;
        }

        .header-menu {
            width: 100%;
            height: auto;

            &__label {
                color: $--grey;
            }

            &__check {
                display: block;
                width: 0.35em;
                height: 0.35em;
                margin: auto 0;
                color: $--grey;
                opacity: 0;
                transition: color 200ms ease-in-out, opacity 100ms ease-in-out;

                &.--checked {
                    opacity: 1;
                }
            }

            &__control {
                width: 100%;
                height: auto;
                padding: 0.35em 0.6em;
                border: 1px solid transparent;
                border-radius: 7px;
                transition: padding-left 200ms ease-in-out, border-color 250ms ease-in-out;

                &:hover {
                    padding-left: 0.8em;
                    border-color: $--light-grey;
                    background-color: transparent;

                    Text, Icon {
                        color: $--light-grey;
                    }
                }
            }

            &__list {
                margin-top: -0.45em;
                margin-left: calc(100% + 0.6em);
            }
        }
    }
}
