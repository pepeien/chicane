<Container class="outliner">
    <DockHeader id="outlinerHeader" label="Outliner" />
    <Container class="outliner__items">
        <Button
            dir:for="actor : outlinerActors"
            class="outliner__item"
            onClick="{{ onItemSelection(actor) }}"
        >
            <Text>{{ actor.getId() }}</Text>
        </Button>
    </Container>
</Container>
