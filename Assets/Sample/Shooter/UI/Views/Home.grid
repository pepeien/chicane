<View path="home">
    <Container dir:if="{{ bDidPlayerWin }}" class="victory">
        <Text>Victory</Text>
    </Container>
    <Sample::Telemetry />
    <Text class="score">Score {{ playerScore }} / {{ maxScore }}</Text>
    <Sample::Crosshair settings="{{ crosshair }}" />
</View>
