#version 460

layout(location = 0) out vec4 outColor;

layout(push_constant) uniform OutlinePush {
    vec2 pixelOffset;
    vec2 padding;
    vec4 color;
} push;

void main() {
    outColor = push.color;
}
